<header class="sticky top-0 z-30 bg-white/80 backdrop-blur border-b">
  <nav class="mx-auto max-w-md px-4 h-14 flex items-center justify-between">
    <a href="/" class="font-semibold tracking-tight">Puzzeltocht</a>
    <div class="flex items-center gap-3">
      <% if (session?.isAdmin) { %>
        <a class="text-sm underline" href="/admin-dashboard">Dashboard</a>
        <a class="text-sm underline" href="/admin-logout">Logout</a>
      <% } else { %>
        <a class="text-sm underline" href="/admin-login">Admin</a>
      <% } %>
    </div>
  </nav>
</header>
